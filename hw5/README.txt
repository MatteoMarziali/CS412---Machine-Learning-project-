The attached .py files can be ruined easily from the terminal from the folder, by calling 'Python <script_name>'. 
So if you want to run the preprocessing just write 'python preprocessing.py'.
To run the training call 'python train.py'.
To run the test call 'python test.py'. 
As the professor suggested me, I delivered the baseline (always predict most frequent classifier) along with the two best models I got (clearly stating which is the best one).
